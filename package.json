{
  "name": "blog",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf ./dist && node ./build/index.js && cp -R ./src/images ./dist",
    "server-dist": "lite-server -c ./build/server-dist.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "handlebars": "^4.0.11",
    "highlight.js": "^9.12.0",
    "markdown-it": "^8.4.0",
    "ncp": "^2.0.0",
    "stylus": "^0.54.5"
  }
}
