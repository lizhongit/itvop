{
  "name": "blog",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf ./dist && node ./build/index.js && cp -R ./src/images ./dist && cp -R ./src/static ./dist",
    "server": "lite-server -c ./build/server-dist.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
  },
  "devDependencies": {
    "cz-conventional-changelog": "^2.1.0",
    "npm-run-all": "^4.1.5",
    "clean-css": "^4.1.11",
    "handlebars": "^4.0.11",
    "highlight.js": "^9.12.0",
    "markdown-it": "^8.4.0",
    "ncp": "^2.0.0",
    "stylus": "^0.54.5"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
